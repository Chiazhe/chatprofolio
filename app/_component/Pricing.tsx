import React from "react";

type Props = {};

const Pricing = (props: Props) => {
  return (
    <div className="h-[200px] bg-gradient-to-b from-card to-black">Pricing</div>
  );
};

export default Pricing;
